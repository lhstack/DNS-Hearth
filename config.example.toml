# FluxDNS 配置文件
# FluxDNS Configuration File
#
# 将此文件复制为 config.toml 并根据需要修改
# Copy this file to config.toml and modify as needed
#
# 注意: 环境变量优先级高于配置文件
# Note: Environment variables take priority over config file
#
# 以下配置通过 Tauri 桌面界面管理，并存储在应用数据库中:
# The following are managed through the Tauri desktop UI and stored in SQLite:
# - DNS 监听器、系统 DNS 绑定和预设地址
# - 上游 DNS 服务器
# - 缓存设置
# - 查询策略

# =============================================================================
# 数据库配置 (Database Configuration)
# =============================================================================

# SQLite 数据库文件路径
# SQLite database file path
database_url = "sqlite:fluxdns.db?mode=rwc"

# =============================================================================
# 日志配置 (Logging Configuration)
# =============================================================================

# 日志文件存储目录
# Log file storage directory
log_path = "logs"

# 日志级别: trace, debug, info, warn, error
# Log level: trace, debug, info, warn, error
log_level = "info"

# 单个日志文件最大大小 (字节)
# Maximum size of a single log file (bytes)
# 默认值: 10485760 (10MB)
log_max_size = 10485760

# 日志保留天数
# Log retention days
log_retention_days = 30
